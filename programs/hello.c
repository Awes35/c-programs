/* The Hello World Program in C
Author: Kollen Gruizenga
 */

/* the 2 libraries we need */
#include <math.h>
#include <stdio.h>

/* the main method, where we run the program from */
int main() {
  printf("Hello World Program, where we are performing a simple math calculation:\n");
  printf("2 to the 10th power is %f\n", pow(2,10)); //pow is used: (denominator, power)

  return 0;
}



